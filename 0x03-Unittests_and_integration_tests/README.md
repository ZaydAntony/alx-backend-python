# Tests for 0x03 - Unittests and Integration Tests

This repository contains unit and integration tests for the `utils` and `client` modules
used in the ALX backend Python project.

Files added:
- `test_utils.py` — unit tests for `utils.py`
- `test_client.py` — unit & integration tests for `client.GithubOrgClient`

Run tests with:
```bash
python3 -m pytest -q
